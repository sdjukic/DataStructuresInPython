Weird thing, we know that inserting elements in the front of the list is not efficient in python.
So what to do when you have a lot of such inserts? Reverse list and append to the end! So simple.
But as always if you have such situation maybe some other datastructure should be better ;)
